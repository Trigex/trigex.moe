package views

templ navLink(href, text string) {
    <a href={ templ.URL(href) } class="btn btn-sm border border-base-300 bg-base-200 px-4 text-sm shadow-sm hover:bg-base-300 sm:btn-md sm:border-transparent sm:bg-transparent sm:shadow-none">{ text }</a>
}

templ Layout(meta PageMeta, component templ.Component) {
    <!DOCTYPE html>
    <html lang="en" data-theme="dracula">
        <head>
            <meta charset="UTF-8"/>
            <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
			<link rel="stylesheet" href="/static/css/site.css"/>
            <link rel="alternate" type="application/rss+xml" title="trigex.moe Blog RSS" href="/blog/rss.xml"/>
            <script src="https://unpkg.com/htmx.org@1.9.12" crossorigin="anonymous"></script>
			<link rel="apple-touch-icon" sizes="180x180" href="/static/img/apple-touch-icon.png">
            <link rel="icon" type="image/png" sizes="32x32" href="/static/img/favicon-32x32.png">
            <link rel="icon" type="image/png" sizes="16x16" href="/static/img/favicon-16x16.png">
			<title>{ meta.Title }</title>
			<meta name="description" content={ meta.Description }>
			<meta name="author" content="Trigex">
            <meta name="robots" content={ meta.Robots }>
            <meta property="og:site_name" content="trigex.moe">
            <meta property="og:title" content={ meta.Title }>
            <meta property="og:description" content={ meta.Description }>
            <meta property="og:type" content={ meta.OGType }>
            if meta.CanonicalURL != "" {
                <link rel="canonical" href={ meta.CanonicalURL }>
                <meta property="og:url" content={ meta.CanonicalURL }>
            }
            if meta.OGImageURL != "" {
                <meta property="og:image" content={ meta.OGImageURL }>
                <meta name="twitter:image" content={ meta.OGImageURL }>
            }
            if meta.PublishedTime != "" {
                <meta property="article:published_time" content={ meta.PublishedTime }>
            }
            <meta name="twitter:card" content={ meta.TwitterCard }>
            <meta name="twitter:title" content={ meta.Title }>
            <meta name="twitter:description" content={ meta.Description }>
            for _, jsonld := range meta.JSONLD {
                @templ.Raw("<script type=\"application/ld+json\">" + jsonld + "</script>")
            }
        </head>
        <body class="min-h-screen overflow-x-clip bg-base-200 text-base-content">
            <main class="mx-auto flex min-h-screen w-full max-w-6xl min-w-0 flex-col px-4 py-6">
                <nav class="navbar flex-col items-stretch gap-3 rounded-box bg-base-100 p-3 shadow sm:flex-row sm:items-center">
                    <div class="flex min-w-0 flex-1 justify-center sm:justify-start">
                        <a href="/" class="btn btn-ghost text-xl">trigex.moe</a>
                    </div>
                    <div class="flex flex-wrap justify-center gap-2 sm:flex-none sm:justify-end">
                        @navLink("/", "Home")
                        @navLink("/music", "Music")
                        @navLink("/projects", "Projects")
                        @navLink("/blog", "Blog")
                    </div>
                </nav>

                <section class="mt-8 min-w-0 flex-1">
                    @component
                </section>

                <footer class="footer footer-center mt-8 rounded-box bg-base-100 p-8 text-base-content shadow">
                        <div class="flex flex-col items-center gap-1 text-center">
                            <p>(c) Trigex 2026, <a href="https://github.com/Trigex/trigex.moe">website source code</a> licensed under the MIT License</p>
                            <p>Email me at <a href="mailto:trigex@trigex.moe">trigex@trigex.moe</a> to contact me</p>
                        </div>
                </footer>
            </main>
        </body>
    </html>
}